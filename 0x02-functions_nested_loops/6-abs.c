#include "main.h"

/**
 * _abs - computs the absolute value of an integer
 * @t: integer must be computed
 * Return: The absolute value
 */

int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
