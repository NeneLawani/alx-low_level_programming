#include <stdio.h>
/**
 * main - Entry point
 * @argc: argument count
 * @argv: array of pointers to the argument strings
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
